# Python related files
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environment
env/
venv/
ENV/
.env
.venv

# Distribution / packaging
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~

# OS generated files
.DS_Store
Thumbs.db

# Generated data and output files
data/*.xlsx
data/*.txt
data/*.json
# Keep data directory itself, but ignore specific generated files above
# If raw/processed subdirs are used, ignore them explicitly:
# data/raw/
# data/processed/

# Report generation artifacts (updated path)
reports/*.tex
reports/*.pdf
reports/*.log
reports/*.aux
reports/*.out
reports/*.toc
reports/*.synctex.gz

# Legacy report path (can be removed after migration)
llm_report_tool/reports/*.tex
llm_report_tool/reports/*.pdf
llm_report_tool/reports/*.log
llm_report_tool/reports/*.aux
llm_report_tool/reports/*.out
llm_report_tool/reports/*.toc
llm_report_tool/reports/*.synctex.gz

# WebDriver related
drivers/
drivers/*.exe
drivers/*.zip
*.crx
node_modules/
chromedriver
undetected_chromedriver.zip

# Test related
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
nosetests.xml
coverage.xml
*.cover

# Security scan reports
bandit-report.json
.secrets.baseline.backup
security-audit.json

# Config files (ignore actual, keep example)
config.json

# But DO NOT ignore example files
!config.json.example
!.env.example

# Claude Code user configuration
.claude/
CLAUDE.md

# Additional development files
.python-version
.pre-commit-config.yaml
# Note: .secrets.baseline is kept (security baseline)
# Note: run_tests.py removed (redundant with Poetry)

# Logs
logs/
*.log
